spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mini-market-db
    username: market
    password: market
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.
  data:
    jdbc:
      dialect: postgresql